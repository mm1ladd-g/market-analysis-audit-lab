# Examples

Run `make demo` to generate a deterministic, fully synthetic workspace. It uses no network, no API key, no real channel, and no real market event. Generated evidence belongs under `workspace/` and is intentionally ignored by Git.

`provided-sources/` documents the preferred local import format. Copy it outside the repository, point `PROVIDED_SOURCES_DIR` at the copy, and replace only with authorized material.
